# Animica test environment (copy to tests/.env or export in your shell)
# These values are used by pytest fixtures in tests/conftest.py.

# -------- Core Network ----------
CHAIN_ID=1337

# HTTP/WS RPC endpoints (devnet defaults)
RPC_HTTP=http://127.0.0.1:8545
RPC_WS=ws://127.0.0.1:8546

# Back-compat aliases some tools expect
RPC_URL=http://127.0.0.1:8545
WS_URL=ws://127.0.0.1:8546

# -------- Studio Services (optional, for deploy/verify/simulate/faucet tests) ----------
SERVICES_URL=http://127.0.0.1:8080
# If faucet is enabled in services, provide a throwaway API key for tests
FAUCET_API_KEY=dev-test-key

# -------- Optional subsystem endpoints (only if split from core RPC) ----------
# Data-Availability microservice (if present)
DA_URL=http://127.0.0.1:8686
# AI/Quantum job fabric (AICF) gateway (if present)
AICF_URL=http://127.0.0.1:8787
# Randomness beacon (if exposed separately; otherwise the core RPC is used)
BEACON_URL=http://127.0.0.1:8888

# -------- Devnet ports (documentation / helpers) ----------
DEVNET_P2P_PORT=30303
DEVNET_RPC_PORT=8545
DEVNET_WS_PORT=8546
DEVNET_SERVICES_PORT=8080
DEVNET_DA_PORT=8686
DEVNET_AICF_PORT=8787
DEVNET_BEACON_PORT=8888

# -------- Test harness inputs ----------
# Funded accounts for tests (used by SDK examples / harness)
ACCOUNTS_JSON=./sdk/test-harness/fixtures/accounts.json

# -------- Timeouts & retries ----------
TEST_RPC_TIMEOUT_SECS=20
TEST_WS_TIMEOUT_SECS=30
TEST_RETRY_ATTEMPTS=3
TEST_RETRY_BACKOFF_SECS=0.5

# -------- Misc toggles ----------
# If true, tests will assume a local devnet; set to false to point at remote endpoints above.
USE_DEVNET=true
# Enable verbose logging in tests (debug/info/warn/error)
TEST_LOG_LEVEL=info

# -------- Notes ----------
# 1) Copy this file to tests/.env and adjust as needed.
# 2) Export vars for local runs:
#      set -a; . tests/.env; set +a
#    or with direnv: place in .env and `direnv allow`.
# 3) CI can inject the same names via job secrets/vars.
