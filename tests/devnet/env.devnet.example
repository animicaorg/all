# Animica Devnet environment (example)
# ---------------------------------------------------------
# Copy this to tests/devnet/.env (or pass with --env-file)
# and tweak as needed. This file documents the default
# ports and suggested host data locations for a local
# devnet stack (nodes + miner + studio-services + explorer).
#
# Once set, bring the stack up via tests/devnet/up.sh to automatically
# restart any stale containers and free the expected host ports.
#
# NOTE: The provided docker-compose.yml in this folder uses
# named Docker volumes by default (node1_data, node2_data,
# services_data). If you prefer bind-mounts to the host
# paths below, change the volumes section in
# tests/devnet/docker-compose.yml accordingly.

# Which compose profile to use
COMPOSE_PROFILES=dev

# Compose project name (isolates containers/volumes)
COMPOSE_PROJECT_NAME=animica-devnet

# -------------------
# Chain / network
# -------------------
ANIMICA_CHAIN_ID=1337

# -------------------
# Host ports (what you connect to on your machine)
# -------------------
HOST_NODE1_RPC=8545          # http://localhost:8545  (primary node RPC/WS)
HOST_NODE2_RPC=9545          # http://localhost:9545  (secondary node RPC/WS)
HOST_STUDIO_SERVICES=8787    # http://localhost:8787  (studio-services API)
HOST_EXPLORER=5173           # http://localhost:5173  (explorer-web preview)

# Convenience URLs derived from ports above
DEVNET_RPC_URL=http://localhost:${HOST_NODE1_RPC}
SERVICES_URL=http://localhost:${HOST_STUDIO_SERVICES}
EXPLORER_URL=http://localhost:${HOST_EXPLORER}

# -------------------
# Miner defaults
# -------------------
MINER_DEVICE=cpu
MINER_THREADS=2

# -------------------
# Optional faucet key (dev ONLY)
# Leave empty to disable faucet in studio-services.
# Example formats: raw hex (0x…), or whatever your service expects.
# -------------------
FAUCET_KEY=

# -------------------
# CORS (dev-friendly)
# Keep wide-open only in local/dev setups.
# -------------------
CORS_ALLOW_ORIGINS=*

# -------------------
# Suggested host data paths (only used if you switch the
# compose file from named volumes to bind mounts)
# -------------------
ANIMICA_NODE1_DATA=./.data/node1
ANIMICA_NODE2_DATA=./.data/node2
ANIMICA_SERVICES_DATA=./.data/services

# -------------------
# Explorer-web build-time env (mirrors explorer-web/.env.example)
# -------------------
VITE_RPC_URL=${DEVNET_RPC_URL}
VITE_CHAIN_ID=${ANIMICA_CHAIN_ID}
VITE_SERVICES_URL=${SERVICES_URL}
