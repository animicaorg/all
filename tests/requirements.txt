# Animica test dependencies (pinned)
# Target Python: >=3.10

pytest==8.3.3
pytest-asyncio==0.23.8
pytest-cov==5.0.0

hypothesis==6.112.1

httpx==0.27.2
websockets==13.0

# Atheris (coverage-guided fuzzing). Only supports CPython <3.12 and non-Windows.
atheris==2.3.0 ; platform_system != "Windows" and python_version < "3.12"

# Playwright for E2E/browser sanity checks.
# After installing, run: `python -m playwright install --with-deps`
playwright==1.47.2
