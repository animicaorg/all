import 'package:flutter/material.dart';

import '../common/placeholder_page.dart';

class CreateMnemonicPage extends StatelessWidget {
  const CreateMnemonicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(title: 'Create Wallet', icon: Icons.edit_note_outlined);
  }
}
