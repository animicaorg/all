# Minimal, EMPIRICALLY TESTED dependency set for running the Animica mainnet
# node (`python -m rpc`) from the PyPI `animica` package WITHOUT the AI stack.
#
# Why this file exists: upstream `animica` declares torch/transformers/
# diffusers etc. as *base* dependencies (the same package powers miners and
# AI providers). A plain `pip install animica` therefore pulls multiple GB of
# CUDA/AI wheels a public RPC node does not need. The Dockerfile installs
# animica with --no-deps and takes exactly this set instead.
#
# Provenance: pip freeze of a Python 3.12 venv (Linux x86_64, 2026-08-03) that
# was verified to boot `python -m rpc` on mainnet (genesis chainId=1,
# ML-DSA-65 self-test ok) and serve /healthz. ~138 MB installed.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
cbor2==6.1.4
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
cryptography==50.0.0
fastapi==0.115.14
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
markdown-it-py==4.2.0
mdurl==0.1.2
prometheus_client==0.26.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
rich==15.0.0
shellingham==1.5.4
starlette==0.46.2
typer==0.27.1
typing_extensions==4.16.0
typing-inspection==0.4.2
urllib3==2.7.0
uvicorn==0.52.1
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
