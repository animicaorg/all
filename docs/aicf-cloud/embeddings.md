# Embeddings API

Endpoint: `POST /v1/embeddings`

- OpenAI-compatible embeddings response list.
- Default model: `aicf-embed-1`.
- Metering includes prompt tokens, vectors generated, bytes, and ANM charge.
