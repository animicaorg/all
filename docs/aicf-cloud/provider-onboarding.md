# Provider Onboarding

1. Sign up with provider role.
2. Register provider profile: `POST /provider/register`.
3. Stake ANM: `POST /provider/stake`.
4. Register node capabilities: `POST /provider/nodes`.
5. Run provider daemon with issued daemon token.
