# Chat Completions API

Endpoint: `POST /v1/chat/completions`

- OpenAI-compatible request/response schema.
- Supports stream mode using SSE chunks.
- Metering includes input/output token counts, latency, bytes, provider, and ANM charge.
- Default model: `aicf-chat-1`.
