# ===== explorer-web environment (copy to .env and edit) =======================
# HTTP JSON-RPC endpoint for your node (FastAPI RPC in this repo).
# Examples:
# - Devnet/local:  http://localhost:8545
# - Testnet:       https://rpc.testnet.animica.xyz
VITE_RPC_URL=http://localhost:8545

# CAIP-2 numeric chain identifier used by the node and wallet (e.g., 1 mainnet,
# 2 testnet, 1337 devnet). Must match the node you connect to.
VITE_CHAIN_ID=1337

# Optional: Explorer API base URL if you run a separate indexer/service layer.
# Leave blank to have the app query the node RPC directly.
# Example: https://explorer-api.testnet.animica.xyz
VITE_EXPLORER_API=

# Optional: studio-services base URL (deploy/verify/faucet/simulate helpers).
# Used for deep-links and artifact lookups in the explorer UI.
# Example local: http://localhost:8090
VITE_SERVICES_URL=http://localhost:8090
# ==============================================================================
# Notes:
# - After editing this file (or your copied .env), restart the dev server.
# - Prefer HTTPS in production and ensure CORS on RPC/services allows the app origin.
